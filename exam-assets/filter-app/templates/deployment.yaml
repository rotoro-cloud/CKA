apiVersion: apps/v1
kind: Deployment
metadata:
  name: filtration-east-app
  labels:
    app: filtration-east-app
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: filtration-east-app
  template:
    metadata:
      labels:
        app: filtration-east-app
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
