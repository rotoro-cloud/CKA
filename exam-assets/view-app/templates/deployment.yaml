apiVersion: apps/v1
kind: Deployment
metadata:
  name: view-global-app
  labels:
    app: view-global-app
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: view-global-app
  template:
    metadata:
      labels:
        app: view-global-app
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
